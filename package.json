{
  "name": "desktop-voice-agent",
  "version": "0.1.0",
  "description": "A desktop voice agent with local STT and LLM processing",
  "author": "Desktop Voice Agent Team",
  "license": "MIT",
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev",
    "build": "tauri build",
    "test": "echo \"Tests will be added in future PRs\" && exit 0"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.0.0",
    "@tauri-apps/plugin-global-shortcut": "^2.0.0",
    "@tauri-apps/plugin-notification": "^2.0.0",
    "@tauri-apps/plugin-shell": "^2.0.0"
  }
}
